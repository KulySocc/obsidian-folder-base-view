# Copy this file to `.env` and set the absolute path to this plugin's folder
# inside your TEST vault. Used by `npm run dev` (Hot-Reload) and `npm run deploy`.
#
# macOS example:
# OBSIDIAN_VAULT_PLUGIN_DIR=/Users/you/Documents/MyVault/.obsidian/plugins/folder-base-view
OBSIDIAN_VAULT_PLUGIN_DIR=
